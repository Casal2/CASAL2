name: Casal2 build Windows install
on:
  push:
    branches:
      - master # age_length_redesign_202109
jobs: 
 Compile-Casal2-Setup:
    runs-on: windows-2022
    timeout-minutes: 180
    steps:
      - name: Checkout Casal2
        uses: actions/checkout@v2

      - name: Checkout r4Casal2
        uses: actions/checkout@v2
        with:
          repository: NIWAFisheriesModelling/r4Casal2
          path: r4Casal2

      - name: Build third-party library - adolc
        run: |
          pwd
          ls -la
          cd ..          
          pwd
          ls -la
          cd ..          
          pwd
          ls -la
                    
